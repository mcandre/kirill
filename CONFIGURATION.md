# CONFIGURATION

kirill uses CLI flags for configuration.

# -5 / --json5

Default: `json`

Enable JSON5 format.

# `-l` / `--list`

List matching file paths.

# `-s` / `--schema` `<path>`

Enforce a given JSON schema.
